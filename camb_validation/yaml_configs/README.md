# Level 2 Cobaya YAML Configurations

Exact YAML files used for Level 2a MCMC chains.

Copy the YAML files from the gaming PC's CAMB_IAM_L2 directory here.

## Files

- `iam_level2a_fixed.yaml` -- L2 Run A: IAM dual-sector, mu_0 = -0.13495 fixed
- `iam_level2a_lcdm.yaml` -- L2 Run C: LCDM baseline
- `iam_level2a_rsd.yaml` -- L2 Run D: IAM dual-sector + RSD likelihoods

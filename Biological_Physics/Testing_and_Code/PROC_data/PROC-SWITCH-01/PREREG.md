# PREREG — PROC-SWITCH-01: the gauge switch (CHAIN_COMMISSIONING row B)

**Sealed 2026-09-21 before any code was changed.** Owner H. W. Mahaffey · Analyst Claude Science.
**What changes.** `cpg_conductor.run_full` currently REPORTS the marker-union gauge (`stage_b_classes`, `gauge_surface='marker_union'`, read against `age_reference_matrix.json` compiled the same way — the defect PROC-N7-01 caught) and emits the identity-loci gauge alongside it against a PROVISIONAL Phase-1 band. After the switch: **the reported class A is the identity-loci gauge** A = H(β̄)/H_min on `iamatlas_gauge_identity_loci_v1_0.json`, on mapped β (Stage 1s), age-referenced (`reference_age_curve_v1.json`) and lab-zeroed (`lab_zero.py`), placed in `identity_band_v3.json`. The marker-union gauge is retained in the bundle as `diagnostic_marker_union` (lineage; never the reported A). Stage 5 (Mahalanobis) and Stage 6 (cellular age) still consume the marker-union statistics and are flagged `pending_recalibration=True` — they are the next commissioning rows, not this one.
**identity_band_v3 (built here, before the tests).** On the four zeroed healthy cohorts (Uppsala, Karolinska, Munich, UCLA; n = 1,379 gated): A″ = A_mapped − c(decade) − z_lab, z_lab = full-cohort median residual; band = p10–p90 of A″ pooled (all decades, since age is removed by c), plus per-decade p10/p90 for reporting. This is the in-sample version of the band PANEL-03 P3″ tested leave-one-lab-out (held 75–84 % of a fourth lab). Provenance stated in the file.
**Tests (fixed).**
- **S1 — the switch is real.** For the seven cached Stage-1 whole-blood arrays (`betas_cache.pkl`: GSE87571 ×3, GSE42861 ×4 incl. 2 RA) `run_full` returns `classes.immune.gauge_surface == 'identity_loci'`, `scale` MAPPED, and — with each array's lab zero supplied (Uppsala 0.000, Karolinska +0.024) — `reportable == True` and `A_abs` present. PASS = 7/7.
- **S2 — UNSET refuses.** The same arrays without a lab zero: `reportable == False`, `lab_zero == 'UNSET'`, no tier printed for the identity gauge. PASS = 7/7.
- **S3 — healthy reads in band.** The 5 healthy arrays among the seven: immune A″ within identity_band_v3 pooled p10–p90. PASS ≥ 4/5 (a p10–p90 band admits 80 %).
- **S4 — N7 on the switched chain.** The synthetic whole-blood cohort (PROC-N7-01 generator, WHOLE_BLOOD_ALPHA, n_hc = 40, atlas-mean β, no disease signal) through `run_full` with the map disabled (synthetic β is already on the atlas scale) and lab zero 0: median immune A″ within ±0.010 of 1.000 and ≥ 70 % in band. This is the exact condition the wired gauge failed on 2026-09-19 (every synthetic healthy far above band).
- **S5 — the separation surface did not move.** `test_a_score_canonical.py` and the GSE51032 sealed-anchor check (PROC-ANCHOR-01, r = 1.00000) unchanged — the switch touches the gauge only.
- **S6 — the four cohorts in band (in-sample, descriptive).** Per-cohort fraction of gated healthy donors with A″ in identity_band_v3: report; expected ≈ 0.80 each.
**Outcome.** S1–S5 pass → row B COMMISSIONED; Stages 5 and 6 open next. Any fail → written as found, switch not merged.

---
**SEALED** sha256 `c1ffa15aacdec79c184d647d374e9658932dfcd1a1cf19f615be7db232cf8d6d` · 2026-09-21
